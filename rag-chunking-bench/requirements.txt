# Python 3.11. Dependencies are added the day they are first used.
pytest==9.1.1
numpy==2.3.1
matplotlib==3.11.1
scikit-learn==1.9.0
tiktoken==0.13.0
# Optional dense-retriever stack (dependency group "dense" in pyproject.toml;
# torch resolves from the CPU-only index there). Not needed for lexical runs.
sentence-transformers==5.6.0
torch==2.12.1
