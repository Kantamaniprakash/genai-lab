name: Sync README digest
on:
  push:
    branches: ["main"]
    # Any project's log can be the current one; ROADMAP.md is what names it,
    # so a flagship handover triggers the digest too.
    paths: ["*/research/NOTES.md", "ROADMAP.md", "scripts/sync_latest.py"]
  workflow_dispatch:
permissions:
  contents: write
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Regenerate the Latest section
        run: |
          python scripts/sync_latest.py --selftest
          python scripts/sync_latest.py
      - name: Commit if changed
        run: |
          if ! git diff --quiet README.md; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git commit -m "Sync README digest with the latest research log entry" README.md
            git push
          fi
